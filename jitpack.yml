jdk:
  - openjdk11

#before_install:
#  - ./custom_setup.sh

install:
#  - echo "Running a custom install command"
  - kotlin/gradlew -p kotlin clean install

#env:
#  MYVAR: "custom environment variable"
